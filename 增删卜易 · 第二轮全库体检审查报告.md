# 增删卜易 · 第二轮全库体检审查报告

**审查者**：monkeycode（巡卦御史）　**委托**：道玄 **范围**：`/workspace` 全库（suanfa.js 1899 行 / shuju.js 2695 行 / zhukong.js 566 行 / jiegua.html 1742 行 / jiegou.html 451 行 / api/qwen.js 190 行 / functions/api/ai.js 169 行 / history.html 399 行 / test/regression.js 281 行） **方式**：只读 + 内存副本（`/tmp/opencode/`）模拟验证，仓库零改动 **基线**：`node test/regression.js` → **443 PASS / 0 FAIL**；64 卦数据完整性（卦名唯一、pattern 唯一、世应合法、爻位天干地支合法、八宫各 8 卦）全部通过

---

## 一、问题分级清单

|级别|编号|问题|位置|
|---|---|---|---|
|**P0 阻断**|A-1|应期引擎用神索引越界（off-by-one）|suanfa.js:1272|
|**P1 必改**|B-1|疾病门用神口径与案例库矛盾（缺「占何人病」分流）|suanfa.js:1482 与 1871-1886|
|**P1 必改**|B-2|摇卦模式起卦→释卦时间漂移|zhukong.js:318-355 → jiegua.html:1157|
|**P2 完善**|C-1|历史记录干支丢失（未释卦恒「—」）|zhukong.js:399|
|**P2 完善**|C-2|AI 代理 CORS `*` + 无 Origin 直连放行，白名单可绕过|functions/api/ai.js:14-28, 63-80|
|**P2 完善**|C-3|双套门类关键词表，用神与原神判定有漂移风险|suanfa.js:1684 vs jiegua.html:942|
|**P3 优化**|D-1|双份首句模板（fixedOpening/expectedOpen）|api/qwen.js:23 vs jiegua.html:1391|
|**P3 优化**|D-2|两套伏神字段命名（中文键 vs 英文键）|jiegou.html:239 vs jiegua.html:1189|
|**P3 优化**|D-3|伏神「真空」判定缺动爻生扶维度|suanfa.js:530|
|**P3 优化**|D-4|出生时辰/姓名明文存 localStorage，缺隐私提示|zhukong.js:366-375|
|**延续**|E-1|婚姻-7 收紧与 regression.js:252 断言冲突（第一轮 P0）|策3方案|
|**延续**|E-2|jinBing 透传缺失（第一轮 P1-1）|zhukong.js:366-375|
|**延续**|E-3|疾病门仇神方向未裁决（第一轮 P1-2）|悬案|

---

## 二、P0 阻断级

### A-1 应期引擎用神索引越界（`suanfa.js:1272`）

```
// 现码（错误）
if (yongShen && typeof yongShen.primaryIndex === 'number') {
    const ysYao = yaoDetail[yongShen.primaryIndex];   // ← 缺 -1
    if (ysYao && ysYao.kongType === '真空' && ysYao.yuePo) weak = true;
}
// 同文件另两处均为正确写法（对照证据）
// suanfa.js:826  jiShenChouShen  → yaoDetail[yongShen.primaryIndex - 1]
// suanfa.js:1327 suanDuanGua 步1 → yaoDetail[yongShen.primaryIndex - 1]
```

**内存副本实测**（三场景，全部错位）：

|场景|构造|期望 weak|实际 weak|
|---|---|---|---|
|A|primaryIndex=1，初爻真空+月破|true|**false**（误取二爻）|
|B|primaryIndex=1，初爻正常、二爻真空+月破|false|**true（误伤）**|
|C|primaryIndex=6，上爻真空+月破|false|**false**（越界 undefined）|

**影响链路**：`weak` 决定 flagsNote「用神真空兼月破：应期当远，出月出旬方应」→ 该标注进入 `yingqi.flagsNote` → qwen.js:97 拼入提示词 → **AI 释文可能引用错误的应期远近**。场景 B 的误伤尤其危险：用神本无真空月破，却对用户断「应期当远」。

**修复**：改 `-1` 一行即可。内存副本修复后全量回归 **443 PASS / 0 FAIL**（regression.js:102 只查结构、不查 weak 语义，故该 bug 逃过回归——建议补断言）。这正是「回归通过≠正确」的典型，需加一条 `tuiYingQi` weak 语义断言。

---

## 三、P1 必改级

### B-1 疾病门用神口径与案例库矛盾（缺「占何人病」分流）

**三方证据冲突**：

- `MENLEI_ZHISHI['疾病'].yongShen = '官鬼'`（suanfa.js:1483）——「以官鬼为病」。
- ANLI 案例库自证相反口径（suanfa.js:1871-1886）：
    - 1873 医占往治章·弟占兄病 → `yongShen: '兄弟'`
    - 1877 疾病章·用化鬼 → `yongShen: '子孙'`（占子病）
    - 1882 疾病章·鬼持世 → `yongShen: '子孙'`，note 明言「**官鬼持世=忧神非病**」
    - 1885 痘疹章·占女儿痘 → `yongShen: '子孙'`
- `MENLEI_RULES` 疾病-8（suanfa.js:1648）已识别「代占官鬼持世为忧神非病」，但**未接线**到取用逻辑。

**矛盾本质**：疾病门把「病」（官鬼）与「所占之人」（用神）两个概念混为一谈。古法占病：占**自病**以官鬼为病察轻重，占**亲病/他人病**以所占之人为用神（占子以子孙、占父以父母、占兄以兄弟），子孙兼为医药。系统问辞「占子病」「占兄病」经 `inferMenLei` 命中疾病门后**一律取官鬼**，与案例库收录的原断南辕北辙。

**建议**：疾病门取用引入「占谁病」维度（自占/代占+亲缘），实现上可在疾病门 `check` 用神前按问辞子串分流（如含「子」「女」→子孙，「父」「母」→父母，「兄弟」「妹」→兄弟，其余→官鬼），并同步修正疾病-2/3/4/7 的用神引用。这是策3疾病门第二批方向反转的**前置项**。

### B-2 摇卦模式起卦→释卦时间漂移

**数据流实锤**：

- `zhukong.js:318-355`：排盘后 `guaData` **只有 `time: new Date().toLocaleString()`（公历字符串）**，无干支 timeInfo。
- `jiegua.html:1156-1164`：释卦时 `if (!timeInfo) timeInfo = getTimeInfo()`——`getTimeInfo()` 取**释卦当下**实时干支。
- 摇卦是主流模式：**起卦与释卦若跨日/跨节气/跨晚子时，月建、日辰、旬空全部用的是释卦时刻**，而非起卦时刻。
- 手动输卦模式正确（jiegou.html:161-186 用户填复盘时辰），但摇卦漂移。

**影响**：timeInfo 是旺衰、空破、用神两现、应期、断卦依据链的**全局地基**。野鹤《应期总注》「神仙难断子时卦」，时辰错则全链错。

**建议**：起卦完成时（zhukong.js `renderFinalResult`）即用 `Solar.fromDate(now).getLunar()` 算出 `{nianGanZhi, yueJian: getMonthZhiExact(), riChen: getDayInGanZhiExact(), xunKong: getDayXunKong(), shiChen: getTimeZhi(), solarTime, lunarTime}` 存入 `guaData.timeInfo`；jiegua.html 优先读 `guaData.timeInfo`，无则才回退实时（保留兜底）。

---

## 四、P2 完善级

### C-1 历史记录干支丢失

- `zhukong.js:399` 写死 `timeInfo: null`。
- `history.html:337` 渲染「月建/日辰/旬空」→ 未释卦的历史**恒显示「—」**。
- 释卦成功后才回写（jiegua.html:1524），且只回写 `solarTime/lunarTime/yueJian/riChen/xunKong`，**缺 shiChen/nianGanZhi**。

**建议**：随 B-2 一并解决——起卦时把完整 timeInfo 存入 history，释卦回写仅作补充。history.html 另加 shiChen 展示。

### C-2 AI 代理白名单可绕过（`functions/api/ai.js`）

- `Access-Control-Allow-Origin: '*'` 恒返回（第 15 行）——校验通过与否，响应头永远 `*`，配合 `Vary` 缺失，任何能发到该端点的请求其响应可被任意站点读取。
- `isAllowedOrigin`（63-80）：**无 Origin 亦无 Referer 的请求直接 `return true`**——curl、Node 脚本、服务端程序可绕过白名单无限调用。
- `AI_GATE_TOKEN` 可选（102 行）：未配置时公开 URL 无鉴权，qwen-max 额度存在盗用面。
- 白名单含 `http://localhost`/`127.0.0.1`：本地恶意脚本可伪装来源。

密钥隔离本身闭环（密钥只在服务端），属**纵深防护缺口**而非泄漏。**建议**：`AI_GATE_TOKEN` 改为必配（部署即锁死）；响应头改回显实际 Origin + `Vary: Origin`；无头直连请求降级为 403 或要求 token。另注意：部署域名变更需同步改 `ALLOWED_ORIGINS`，否则同源 POST（携带 Origin）会被 403。

### C-3 双套门类关键词表漂移

- suanfa.js:1684 `MENLEI_KEYWORDS`（13 门）→ 决定用神/断法/评分。
- jiegua.html:942 `inferQuestionType`（14 对）→ 决定原神判定（`jiSuanYuanShenKongFu`，1268 行）。
- 两表各自迭代命中，顺序不同。当前比对逐词一致（`子孙`→子嗣胎孕、`平安`→趋避防灾等均对齐），但**无单源保障**，改一漏二即用神与原神判定分叉。

**建议**：jiegua.html 直接复用 suanfa.js 的 `inferMenLei`（suanfa.js 已全局加载），删除本地重复表；原神判定改传 menlei。顺带消除 E-1 中 qTypeRW8 与 menlei 可能不一致的隐患。

---

## 五、P3 优化级

- **D-1 双份首句模板**：api/qwen.js:23 与 jiegua.html:1391 各自定义 `fixedOpening/expectedOpen`，改一处忘一处首句校正会兜底自愈但语义漂移。建议 jiegua.html 读 `window` 上 qwen.js 暴露的常量。
- **D-2 两套伏神字段命名**：jiegou.html:239 `{liuqin,dizhi,yaoWei}` vs jiegua.html:1189 `{六亲,地支,伏神爻位,...}`，各自消费正确但同构性差，建议统一中文键。
- **D-3 伏神「真空」判定缺动爻生扶**：suanfa.js:530 只判 `yueKe && !yueSheng && !riSheng`，显爻版（507 行）有 `dongSheng` 维度——伏神判真空偏保守，建议对齐。
- **D-4 隐私边界**：姓名/出生时辰/占问全文明文存 localStorage，`help.html` 缺「本地存储不上云」提示。出生时辰属敏感生物信息，建议指南页明示。

---

## 六、后续完善优化方案（落地路径）

**第一批（止血，2 处改动 + 1 条断言）**

1. 修 A-1：suanfa.js:1272 `-1`；回归补「用神真空兼月破→weak=true、用神正常二爻异常→weak=false」两条断言。
2. 修 B-1 前置：疾病门取用按问辞分流占谁（子/女→子孙，父/母→父母，兄/妹→兄弟，缺省→官鬼），并核 ANLI 四个疾病案例全部自洽。

**第二批（数据链补齐，2 处）** 3. 修 B-2 + C-1：zhukong.js 起卦即存完整 timeInfo（含历史），jiegua.html 优先消费。 4. 修 C-2：AI_GATE_TOKEN 必配 + 回显 Origin/Vary + 无头请求收紧。

**第三批（收敛与打磨）** 5. 修 C-3/D-1/D-2：词表单源化、首句模板单源化、伏神字段统一。 6. 疾病门 UI 二级选（近病/久病）落地到 `MENLEI_4`；策3第二批方向反转（含仇神 P1-2 裁决）在 B-1 完成后实施。

**验收**：每批完成跑 `node test/regression.js` 须 443 PASS 不降（A-1 修复已验证）；新增断言随改动入库；ANLI 疾病四案例与算法取用逐条对照通过。

---

**总体判断**：架构清晰、数据层严谨（64 卦零异常）、XSS 面闭合、密钥隔离闭环。本轮真问题集中在「一个索引越界 + 两个口径断层」——A-1 是硬 bug 应立即修，B-1/B-2 属术数正确性断层，三者修复成本均低且已给出精确落点。回归基线 443 PASS 可作改动护栏。

对 E-1（婚姻-7 收紧）与 E-2（jinBing 透传）维持第一轮结论：E-1 收紧必破坏 regression.js:252，属 P0 需先改断言再改规则；E-2 需在 zhukong.js:366-375 补 `jinBing` 字段透传，否则起卦端选择到释卦端即丢失。