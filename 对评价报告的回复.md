## 八短逐条核实

|#|方案声称|仓库核实|结论|
|---|---|---|---|
|短一|吉凶判定等权 ±1|属实：`suanDuanGua` 全部 `score++/--`；`jiWangShuaiScore` 的 +30/-20 权重在第 1292 行被拍扁成符号（`sc.score>0?1:-1`）|✅ 成立|
|短二|应期只到日、候选不裁决|属实：`tuiYingQi` 扫 365 天、窗口自明日（`i=1` 起）、每类 `cand.length<3`，跨类型无全局排序、无冲突标注|✅ 成立|
|短三|门类子串误判|属实：`MENLEI_KEYWORDS` 为子串匹配，"找"确在失物组；"财"会误命中理财/发财类问辞|✅ 成立|
|短四|原案例仅 1 例|属实：`ANLI` 只录风天小畜一条|✅ 成立|
|短五|两现取舍仅"舍空取实"|**不实**：`xuanYongShen` 已有完整排序链 ①舍闲取世→②舍静取动→③舍破取全→④舍空取实→⑤舍伤取安→⑥旺相优先，"动爻参与取舍"已在②|❌ 前提错误|
|短六|断卦链无前端展示|属实：`jiegua.html` 仅计算（1180）与存盘（1196），无渲染面板，`buildYingqiHtml` 有而断卦面板无|✅ 成立|
|短七|AI 返回文本 XSS|**不成立**：`mdToHtml` 行内经 `mdInline` 已先转义 `& < >` 再做 markdown 替换（728-737 行），`<script>` 会被转义为 `&lt;script&gt;`，无法注入|❌ 前提错误|
|短八|rw7 回归覆盖不足|部分属实：`test/regression.js` 已覆盖真空/假空、六冲六合、两现原例，但动散、"动不为空"等边界无断言|⚠️ 部分|

## 路线图可行性意见

**P0 判定合理，但有一处应剔除：**

- P0-1 断卦面板：可行且价值最高，是本次最有说服力的建议。`duanGua.chain` 数据齐备，新增一个 `buildDuanGuaHtml` 即可与 `buildYingqiHtml` 并排渲染，成本低。
- P0-2 安全收口：**无需做**。短七前提错误，`mdInline` 已转义。建议改为补一条回归断言（喂 `<script>` 测 `mdToHtml` 输出不含可执行标签），一次性加固而非改写。
- P0-3 应期裁决："最近优先排序 + 冲突标注"可行且必要；"月/季/年维度"建议降级到 P1——《增删卜易》远期规则主观性强，需先立"旺相应远/休囚应近"的具体判定标准再编码，否则易成臆断。

**P1 两处需修正：**

- P1-4 层级权重：方向正确，但影响面比方案设想大——会破坏 `r3-verify.js`（吉5/凶-6/中0）与 `test/regression.js` 冒烟断言，须先出权重规格表、再编码、再同步回归。建议权重表落成文档（如用神月破=-3、世应相冲=-1）避免各判据权值成为拍脑袋数字。
- P1-5 门类扩 20+：技术上简单，但 **R4 规格已定稿 12 门类**（`requirements.md:170-181`），扩充属需求变更，必须先更新 requirements/design 再动代码，否则规格与实现脱节。
- P1-6 两现细则：前提错误，建议改为"补回归用例"而非重构——当前缺的是两现皆空、一空一破等边缘断言。

**P2 可行，唯一风险在史料准确性：**

- P2-7 案例扩充：直接扩 `ANLI` 数组即可，但"原断语"必须逐字转录《增删卜易》原文并标注章句出处，杜撰原文会污染对照与回归基线，比不录更糟。
- P2-9 质疑复核机制：依赖 P0-1 断卦面板先行落地，顺序正确。

## 结论

方案方向正确、优先级排序合理，但含两处基于过时或未读代码的误判（短五、短七），照单全收会浪费一次提交。建议：P0 聚焦"断卦面板 + 应期排