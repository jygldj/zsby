# 策3 第二批 落地改动方案（供第三方核验）

> 拟稿：关云长（墨言 WorkBuddy）｜受托外审：monkeycode（第三方评审）
> 时点：2026-08-14（丙午年 丙申月）｜依据：军师令「分类占断扩深·四门精化」策3 第二批
> 目标仓库：`jygldj/zsby`（本地工作区 `/f/github-dx/zsby/`，线上 `origin/main` 为基准）
> 前置：第一批（A1/A2/A3/B1 + 三方核验三点修正）已落定提交 `f20c43e「策3落地」`，`node test/regression.js` 全绿。

---

## 〇、总原则：第二批风险高于第一批，须逐子战斗、逐项可验

| 子战 | 范围 | 牵动 | 风险 | 本批处理 |
|---|---|---|---|---|
| **B-1** | 疾病门吉凶方向反转 | `suanfa.js` 步2（用神旺衰±2）、步5（忌神/原神±1）主体 | **高**（反转全局计分符号） | 本批核心，必含回归锁 |
| **B-2** | 近病/久病 UI 二级选项 + 空亡分流 | `jiegou.html`+`jiegua.html` 输入、`suanfa.js` 步2 空亡分支 | 中 | 与 B-1 协同，缺省保守 |
| **B-3** | 疾病门 `MENLEI_RULES` 计分激活 | `suanfa.js` `MENLEI_RULES` 疾病门段 | 中 | 纠正 疾病-9 方向，余去重 |
| **B-4** | 婚姻门驿马引擎 | 新增 `suanfa.js` 驿马函数 + 婚姻-7/新增规则 | 低 | 年支起驿马，降级不报错 |

> **为何第二批单列**：疾病门用神=官鬼=病，与求财/功名/婚姻「用神旺相=吉」相反；反转牵动步2/步5 全局计分符号，稍有不慎即破坏 R0–R4 与既有 ANLI 自洽。故第一批只做「语义显式化（score=0）」，方向反转留本批专项。

> **对第一版方案的两处更正（外审须知悉）**：
> 1. 第一版方案 B2 称「`zhukong.js` 写入 `userInfo.jinBing`」——**本仓无 `zhukong.js`**。真实承载为 `jiegou.html`（所问之事输入区）与 `jiegua.html`（`userInfoAreaJG` 组装、派发 `guaInfo.userInfo`，见 `jiegua.html:1258`）。本批 UI 改动落此二文件。
> 2. 第一版方案称「疾病门 score=0 仅语义显式化」——本批将其激活为实际计分（与 B-1 反转协同、去重不双计）。

---

## 一、改动总览

| # | 文件 | 位置（注释锚点） | 改动 |
|---|---|---|---|
| B-1a | `ly/suanfa.js` | 步2（约1330–1343行 `if (yongYao)` 块） | 疾病门 `menlei==='疾病'` 时旺衰±2 取反；近病空亡+1 / 久病空破−2 分流 |
| B-1b | `ly/suanfa.js` | 步5（约1387–1409行 看忌仇元神） | 疾病门 忌神有力+1、忌神无力−1、原神得力−1、原神空伏+1（全部取反） |
| B-2a | `jiegua.html` | `userInfoAreaJG`（约653行） | 增「近病/久病」二级选（仅所问=疾病时出现），值→`userInfo.jinBing` |
| B-2b | `jiegou.html` | 所问之事区（约49–58行） | 同步二级选，保证两端一致 |
| B-3 | `ly/suanfa.js` | `MENLEI_RULES` 疾病门段（约1622–1652行） | 激活专属计分；纠正 疾病-9 方向；近病逢冲/逢空 fugai+1 |
| B-4a | `ly/suanfa.js` | `MENLEI_RULES` 之后（约1653行 `];` 后） | 新增 `suanYiMa(timeInfo)` 年支起驿马函数 |
| B-4b | `ly/suanfa.js` | `MENLEI_RULES` 婚姻-7（约1617行） | 扩展为「用神临驿马暗动=心去难留」−1；可加 婚姻-8 驿马发动 |
| B-5 | `test/regression.js` | 文件末（策3 断言区之后） | 新增疾病反转/近久病/驿马回归断言 |

---

## 二、B-1 步2/步5 疾病门符号反转（核心，供核验）

### B-1a 步2 察旺衰（改后骨架）

```javascript
// 步2 察旺衰
const isDisease = guaInfo.menlei === '疾病';
const jinBing = (guaInfo.userInfo && guaInfo.userInfo.jinBing) || ''; // '近病' | '久病' | ''
if (yongYao) {
    const sc = jiWangShuaiScore(yongYao, guaInfo);
    const wangShuai = sc.score > 0 ? '旺相' : (sc.score < 0 ? '休囚' : '平');
    add('察旺衰', '用神' + yongYao.dizhi + ' ' + wangShuai + '(评分' + sc.score + ')', sc.detail || '月日生克综合');
    // 疾病门方向反转：病旺=凶、病休囚=吉（牵动通用±2主体）
    let wSign = (sc.score > 0 ? 2 : (sc.score < 0 ? -2 : 0));
    if (isDisease) wSign = -wSign;
    score += wSign;
    // 月破：疾病门——近病逢冲(月破)即愈→吉；久病/未填→维持凶（口径见 D-a）
    if (yongYao.yuePo) {
        if (isDisease && jinBing === '近病') { add('察旺衰','近病用神月破','近病逢冲即愈'); score += 2; }
        else { add('察旺衰','用神月破','月建'+yueJian+'冲'+yongYao.dizhi+'，根枯难用'); score -= 2; }
    }
    // 空亡：近病逢空即愈(+1)；久病逢空不治(真空−2/假空−1)；未填近久病→保守按久病
    if (yongYao.kongType === '真空' || yongYao.kongType === '假空') {
        if (isDisease && jinBing === '近病') { add('察旺衰','近病用神逢空','近病逢空即愈'); score += 1; }
        else if (isDisease && (jinBing === '久病' || !jinBing)) {
            add('察旺衰','久病用神逢空','久病逢空不治'); score -= (yongYao.kongType==='真空' ? 2 : 1);
        } else {
            if (yongYao.kongType === '真空') { add('察旺衰','用神真空','旬空无生扶'); score -= 2; }
            else { add('察旺衰','用神旬空','逢空待出空'); score--; }
        }
    }
}
```

### B-1b 步5 看忌仇元神（改后骨架，仅疾病门取反）

```javascript
// 步5 看忌仇元神
if (guaInfo.jiShenState && guaInfo.jiShenState.liuqin) {
    const js = guaInfo.jiShenState;
    add('看忌仇元神','忌神'+js.liuqin+(js.positions.length?('在第'+js.positions.join('、')+'爻'):''), js.duanYu || '');
    // 疾病门：忌神=子孙(克官鬼=病)，忌神有力=制鬼=吉(+1)；无力=病无制=凶(−1)
    if (js.wangShuaiScore && js.wangShuaiScore.index > 0) { add('看忌仇元神','忌神有力','忌神旺相克用'); score += (isDisease ? 1 : -1); }
    else { add('看忌仇元神','忌神无力','忌神休囚'); score += (isDisease ? -1 : 1); }
}
// 原神（疾病门：原神=父母生官鬼=病得生=凶，故取反）
if (guaInfo.yuanShenState) {
    const ys = guaInfo.yuanShenState;
    const ysState = ys.isFuCang ? '伏藏' : (ys.isKong ? '旬空' : '得力');
    add('看忌仇元神','原神'+(ys.liuqin||'')+ysState, ys.duanYu || '');
    // 疾病门：原神得力→生官鬼(病)→凶(−1)；原神空伏→不生病→吉(+1)
    if (ys.isKong || ys.isFuCang) score += (isDisease ? 1 : -1);
    else score += (isDisease ? -1 : 1);
}
```

> 注：`isDisease`/`jinBing` 须在 步2 之前（`if (yongYao)` 之外）声明一次，步5 复用；`guaInfo.userInfo` 当前由 `jiegua.html:1258` 派发，B-2 负责写入 `jinBing`。非疾病门（求财/功名/婚姻）`isDisease=false`，计分与原逻辑逐字节一致——**不破坏现有 R0–R4 / ANLI 自洽**。

---

## 三、B-2 近病/久病 UI 二级选项 + 分流

**`jiegua.html` `userInfoAreaJG`（约653行）** 增：

```html
<div class="field-group" id="jinBingGroup" style="display:none;">
  <label for="jinBingSel">近病/久病</label>
  <select id="jinBingSel">
    <option value="">（不确定）</option>
    <option value="近病">近病（新发、急症）</option>
    <option value="久病">久病（宿疾、沉疴）</option>
  </select>
</div>
```

- 联动：当 `jgQuestion`（或所问之事）命中「疾病」时 `jinBingGroup` 显示，否则隐藏（与 B-3 中 `inferMenLei` 同源判定）。
- 取值写入 `userInfo.jinBing`（约1143行 `userInfo.question = ...` 后增 `userInfo.jinBing = jinBingSel ? jinBingSel.value : '';`）。
- **`jiegou.html`** 所问之事区（约49–58行）同步同样二级选，保证手动输卦入口一致。
- 缺省语义：`jinBing===''`（用户未选）→ B-1 空亡分支按「保守=久病」处理（凶向不漏判），不报错。

---

## 四、B-3 疾病门 `MENLEI_RULES` 计分激活 + 方向纠正

第一批疾病门 9 条全 `score=0`（仅语义）。本批与 B-1 反转协同，**通用机制已覆盖者保持 `tongyuan` 去重；通用未覆盖者才 `fugai` 专属计分**：

| 规则 | 第一批 | 本批 | 理由 |
|---|---|---|---|
| 疾病-1 子孙制鬼 | tongyuan/0 | **tongyuan/0**（去重） | 已由 B-1b 忌神有力+1 覆盖 |
| 疾病-2 官鬼旺相(病重) | tongyuan/0 | **tongyuan/0**（去重） | 已由 B-1a 病旺−2 覆盖 |
| 疾病-3 近病用神旬空即愈 | fugai/0 | **tongyuan/0**（去重） | 已由 B-1a 近病空亡+1 覆盖 |
| 疾病-4 久病用神空破不治 | tongyuan/0 | **tongyuan/0**（去重） | 已由 B-1a 久病空破−2 覆盖 |
| 疾病-5 六冲近病即愈 | tongyuan/0 | **fugai/+1**（仅近病） | 通用六冲−1 与「近病逢冲即愈」反向，须专属+1 抵冲（仅 `jinBing==='近病'`） |
| 疾病-6 六冲久病不治 | tongyuan/0 | **tongyuan/0**（去重） | 通用六冲−1 已合久病凶向 |
| 疾病-7 用神化鬼(慎防不测) | tongyuan/0 | **tongyuan/0**（去重） | 通用用神化鬼−2 已覆盖 |
| 疾病-8 代占官鬼持世(忧神非病) | tongyuan/0 | **tongyuan/0**（去重） | 门类特有语义，不计分 |
| **疾病-9 忌神动生元神生用神** | tongyuan/0（注「化凶为吉」） | **tongyuan/0，并纠正注释** | ⚠️ 第一批注释方向错：疾病门 子孙(忌神)生父母(原神)生官鬼(病)=病得连环生助=**凶**。本批改注释为「化吉为凶」，去重（B-1b 原神得力−1 已覆盖） |

> 关键：`fugai` 仅 疾病-5 一项（且限近病），避免与 B-1 反转双计；其余 `tongyuan` 仅 push 语义判据、不改 `score`。

---

## 五、B-4 婚姻门驿马引擎（低危险）

**新增函数**（约1653行 `MENLEI_RULES];` 之后）：

```javascript
// 年支起驿马：申子辰→寅，巳酉丑→亥，寅午戌→申，亥卯未→巳
const YI_MA = { '申':'寅','子':'寅','辰':'寅', '巳':'亥','酉':'亥','丑':'亥', '寅':'申','午':'申','戌':'申', '亥':'巳','卯':'巳','未':'巳' };
function suanYiMa(timeInfo) {
    const nianGanZhi = (timeInfo && timeInfo.nianGanZhi) || '';
    const nianZhi = nianGanZhi.length >= 2 ? nianGanZhi.charAt(1) : '';
    return YI_MA[nianZhi] || null; // 年支缺失→null（降级，不报错）
}
```

**婚姻-7 扩展**（约1617行）：用神临驿马且暗动 → 心去难留：

```javascript
{ id:'婚姻-7', menlei:'婚姻', desc:'用神临驿马暗动（心去难留）', direction:'凶', overlap:'fugai', score:-1,
  check:(g)=>{ const y=g.yongShen; if(!y||typeof y.primaryIndex!=='number') return false;
    const yiMa = suanYiMa(g.timeInfo); if (!yiMa) return false;
    const a=(g.yaoDetail||[])[y.primaryIndex-1];
    return !!(a && (a.dizhi===yiMa) && (a.riPoOrAnDong==='暗动' || a.isDong)); },
  reason:'驿马主动，用神临马而暗动/发动=心去难留（婚姻门特有）。年支缺失时降级不触发' },
```

> 可选 婚姻-8：用神临驿马（不论动静）显式语义 `tongyuan/0`。B-4 不牵动步2/步5，风险最低。

---

## 六、B-5 回归断言（供核验，追加 `test/regression.js` 末）

```javascript
// ===== 策3 第二批：疾病门方向反转 + 近久病 + 驿马 =====
// 1. 疾病：病旺(官鬼旺)→凶（反转后 score 显著为负）
let gD = { menlei:'疾病', userInfo:{}, yongShen:{liuqin:'官鬼', primaryIndex:2, dizhi:'午', wangShuaiScore:{index:1}},
  shiYaoIndex:2, yaoDetail:[{dizhi:'辰',liuqin:'父母'},{dizhi:'午',liuqin:'官鬼',kongType:'none'},{dizhi:'申',liuqin:'兄弟'},
    {dizhi:'戌',liuqin:'父母'},{dizhi:'子',liuqin:'妻财'},{dizhi:'寅',liuqin:'子孙'}],
  jiShenState:{liuqin:'子孙',positions:[5],wangShuaiScore:{index:1}}, yuanShenState:{liuqin:'父母',isKong:false,isFuCang:false},
  guaXiang:{liuChong:false}, timeInfo:{yueJian:'午',riChen:'丙午',nianGanZhi:'丙午'} };
suanDuanGua(gD);
assert(gD.duanGua.score < 0 && (gD.duanGua.jiXiong==='凶'||gD.duanGua.jiXiong==='小凶'), '策3-2 疾病·病旺反转=凶(实际'+gD.duanGua.score+')');

// 2. 疾病：子孙制鬼(忌神有力)→吉向（score 较「无子孙制鬼」更高，即反转 +1 生效）
// 3. 疾病·近病逢空即愈：用神旬空 + jinBing='近病' → 不扣反 +1
// 4. 疾病·久病空破不治：用神真空 + jinBing='久病' → 凶（−2）
// 5. 婚姻·用神临驿马暗动：年支申子辰→寅，用神寅木暗动 → −1
```

> 断言 2–5 以「与同卦非疾病门对比」锁定反转方向，杜绝误判；**全部断言不得触碰现有 25 例 ANLI 与 R0–R4 断言**（非疾病门路径未变）。

---

## 七、风险点与口径决策（请道玄 + monkeycode 裁决）

| # | 决策点 | 末将建议 | 风险 |
|---|---|---|---|
| **D-a** | 疾病门「月破」是否随近病逢冲即愈反转 | 仅**近病**月破→+2（愈）；**久病/未填**月破→维持−2（凶）。理由：近病逢冲即愈，月破亦冲 | 中（月破力度强，近病是否一概即愈存古法分歧） |
| **D-b** | 反转实现：步内 `if(isDisease)` 分支 vs 抽 `reverseSign` | **步内分支**（注释清晰、易审查、不引入新抽象） | 低 |
| **D-c** | `jinBing` 缺省（老数据/其他入口未选） | 缺省按「**久病/保守**」处理（凶向不漏判），不报错 | 低 |
| **D-d** | 驿马 年支缺失 | `suanYiMa` 返 `null`，婚姻-7 降级不触发，不报错 | 低 |
| **D-e** | 疾病-9 注释方向 | 第一批注释「化凶为吉」**错误**，本批改为「化吉为凶」并去重 | 低（仅注释+去重语义） |
| **D-f** | B-1 是否分两小批（先反转后 UI） | 建议**单批**，因 B-2 空亡分流依赖 B-1 的 `isDisease/jinBing` 变量；若拆分须保证变量先声明 | 低 |

---

## 八、验收标准（第二批）

1. `node test/regression.js` 全绿（含新增疾病反转/近久病/驿马断言，且不红现有 25 例 ANLI 与 R0–R4）；
2. 疾病门断语因门类而异：**病旺显示「凶」、子孙持世/制鬼显示吉向**；**近病逢空即愈判吉、久病空破不治判凶**；
3. 婚姻门：用神临驿马暗动显示「心去难留」专属判据（年支齐时）；
4. 去重护栏仍生效（`tongyuan` 规则只 push 判据、不改 `score`）；
5. UI：所问=疾病时现「近病/久病」二级选，写入 `userInfo.jinBing` 并真实影响判定。

---

## 九、第三方核验关注点（给 monkeycode）

1. **符号反转正确性**：步2/步5 取反后，非疾病门计分须与第一批（或 f20c43e）逐字节一致——请重点比对 求财/功名/婚姻 路径未被 `isDisease` 分支影响。
2. **双计风险**：B-1 已计的项（病旺、忌神有力、原神得力、近病空亡、久病空破），在 B-3 `MENLEI_RULES` 疾病门须为 `tongyuan`（仅语义、不改分）；唯 疾病-5（近病逢冲）为 `fugai` 且限近病——请核验无重复加减。
3. **缺省安全**：`jinBing` 未填时不得崩、不得误判吉（保守凶向）。
4. **锚点漂移**：本方案行号基于 f20c43e 后实际文件；按注释锚点（函数名/`MENLEI_RULES`/`userInfoAreaJG`）插入，勿硬插行号。
5. **第一版更正**：`zhukong.js` 不存在 → UI 改 `jiegou.html`+`jiegua.html`；疾病门本批才激活计分。

---

*本方案为第二批完整蓝图，供 monkeycode 以可读权逐条核验 B-1～B-5。未经道玄与第三方确认，关云长不动刀。*
